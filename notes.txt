NOW IN PRESENT I AM LOGGED IN AS sahilbhatiapcacc-cmyk(MY PERSONAL GITHUB ACC)

GIT CODE & NODE is not a repository ...... GIT_Code_-_Notes is a repository so the (.git) file will be in the repo only 

Git is just a version control system that helps to track code.



Commands to chnage the user name or user email:
1. git config global user.name "user_name"
2. git config global user.email "user_email_id_name"



COMMAND TO CHANGE THE DIRECTORY: 
1. cd .. (for exit from current directory)
2. cd <directory name> 



COMMANDS TO PULL :
1. git pull (do it everytime before uploading comitting)



COMMANDS TO PUSH :
1. git add . ( . means to add al the files not indivisually one)
2. git commit -m "your commit comment"
3. git push 



COMMANDS TO PUSH WHEN A NEW REPO IS MADE(TO CHANGE THE BRANCH , SELECT THE REMOTE):\
1.to change the remote repo => git remote add origin <link>
2.git remote -v (to check the remote)

TO WORK WITH BRANCH :
1. git branch (to check what branch are we in)
2. gir branch -M <new name> (new name => main,earlier it can be "master")(TO RENAME THE BRANCH NAME)
3. git push -u origin main (-u means after this code in future it will always work in origin main)
4. git checkout <branch name> (TO NAVIGATE BETWEEN BRANCH)
5. git checkout -b <new branch name> (TO CREATE NEW BRANCH)
6. git branch -d <branch name> (TO DELETE BRANCH)


TO UNDO THE CHNAGES[AFTER ADDING] :
1. git reset <file name> (to reset all files at once then "git reset")

TO UNDO THE CHANGES [AFTER COMMITTING]:
1. git reset HEAD~1 (for one commit change)
2.